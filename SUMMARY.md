# สรุปงาน — เว็บ Landing Page แฟรนไชส์ TARUKA (ทารุกะ)

> เอกสารสรุปสิ่งที่ทำทั้งหมดในโปรเจกต์นี้ · อัปเดตล่าสุด 28 มิ.ย. 2026

---

## 1. ภาพรวมโปรเจกต์

เว็บ **landing page หน้าเดียว** โปรโมทแฟรนไชส์ชานมไข่มุก/เครื่องดื่มแบรนด์ **TARUKA**
เป้าหมายเพื่อขายแฟรนไชส์ — แสดงจุดเด่นแบรนด์ งบลงทุน ตารางคืนทุน รูปสาขาจริง และปุ่มติดต่อ

- รูปแบบ: **เว็บ static ล้วน** (HTML/CSS/JS ไม่มีฐานข้อมูล/ระบบล็อกอิน)
- แก้เนื้อหาทั้งหมดในไฟล์เดียว → `default-content.js`
- โฮสต์ฟรีด้วย **Firebase Hosting**
- ภาษาไทย · mobile-first · โทนแบรนด์ครีม/น้ำตาล/ส้ม

---

## 2. สิ่งที่ทำ (ไทม์ไลน์การตัดสินใจ)

| ลำดับ | สิ่งที่ทำ | ผลลัพธ์ |
|---|---|---|
| 1 | สร้างเว็บ landing + ระบบ admin (Firebase) ตามแผนแรก | หน้าเว็บ + หน้าแก้เนื้อหาผ่าน Firestore/Auth |
| 2 | เพิ่มระบบอัปโหลดรูปเข้า Firebase Storage | ปุ่มอัปโหลด + วางลิงก์ในหน้า admin |
| 3 | เพิ่ม section "เมนู" แบบ carousel | สไลด์รูปอัตโนมัติ |
| 4 | เพิ่ม "โหมดดูตัวอย่าง" (preview ไม่ต้องตั้ง Firebase) | เปิดดู/แก้หน้า admin ผ่าน localStorage |
| 5 | ใส่เนื้อหา + รูปจริงจากโฟลเดอร์ `data/` | เนื้อหา/ตัวเลขจาก Canva + รูปสาขาจริง 12 รูป + โลโก้ |
| 6 | **ตัดระบบ admin/Firebase ออก → เว็บ static** | ลบ admin/Firestore/Auth/Storage แก้เนื้อหาในโค้ดแทน |
| 7 | เปลี่ยนฟอนต์เนื้อหาเป็น Google Sans | heading=Kanit, body=Google Sans+Prompt(ไทย) |

> **เหตุผลที่เปลี่ยนเป็น static (ข้อ 6):** เจ้าของเลือกแก้เนื้อหาในโค้ดเองแทนการใช้หน้า admin
> ทำให้ระบบเรียบง่ายขึ้นมาก (ไม่ต้องตั้ง Firebase/ผูกบัตร) — แลกกับการที่เจ้าของร้านแก้เองไม่ได้
> (ทุกการแก้ต้องแก้โค้ดแล้ว deploy ใหม่)

---

## 3. โครงสร้างไฟล์ปัจจุบัน

```
taruka-landing/
├─ index.html          # หน้าเว็บทั้งหมด (HTML + CSS + JS render)
├─ default-content.js  # ⭐ เนื้อหาทั้งหมด — แก้ที่นี่ที่เดียว
├─ assets/             # รูปที่ใช้บนเว็บ (deploy ไปด้วย)
│   ├─ logo.png        #   โลโก้หัวกวาง
│   └─ 1.jpg … 12.jpg  #   รูปสาขาจริง (ย่อขนาดแล้ว)
├─ data/               # ไฟล์ต้นฉบับ (Canva + รูปเดิม) — ไม่ deploy เก็บอ้างอิง
├─ firebase.json       # ตั้งค่า Firebase Hosting (อย่างเดียว)
├─ .firebaserc         # project id สำหรับ deploy
├─ .gitignore
├─ README.md           # คู่มือใช้งาน/แก้/deploy
└─ SUMMARY.md          # เอกสารนี้
```

---

## 4. Section บนหน้าเว็บ (ตามลำดับ)

1. **Header** — โลโก้กวาง + ชื่อแบรนด์ + ปุ่ม "สนใจแฟรนไชส์"
2. **Hero** — หัวข้อใหญ่ + คำโปรย + ปุ่ม CTA (พื้นหลังรูปร้าน)
3. **About** — "TARUKA ชานมไข่มุก" + แนะนำแบรนด์ (50+ เมนู)
   - **เมนูของเรา** — แบ่งเป็น 6 คอลเลกชัน (โปสเตอร์ + รายการเครื่องดื่มสไตล์เมนู ชื่อ···ราคา)
4. **จุดเด่นของแบรนด์** — การ์ดสถิติ (19฿ / 50+ เมนู / 50+ สาขา / 2 รูปแบบร้าน)
5. **รูปแบบร้าน** — KIOSK (2×2 ม.) / CAFE พร้อมรูป
6. **สาขาของเรา** — carousel รูปสาขาจริง 12 รูป (เลื่อนอัตโนมัติ/ปัด/ลูกศร/จุด)
7. **งบลงทุน** — ตัวเลขจริงจาก Canva
8. **ประมาณการคืนทุน** — ตาราง 9 คอลัมน์ (ขาย 100–500 แก้ว/วัน) เลื่อนแนวนอนได้
9. **การสนับสนุนจากเรา** — โกดัง/อบรม/POS/อุปกรณ์
10. **ติดต่อ** — ปุ่ม LINE + Facebook
11. **Footer** + ปุ่มลอย "ทักไลน์" (sticky บนมือถือ)

> section "เมนู" แบ่งเป็น 6 คอลเลกชัน (MY BOBA MOOD, CREAMY FRUIT KISS, SIGNATURE PURPLE LATTE,
> AIYU JELLY FRESH VIBES, MUST TRY, SIGNATURE MATCHA) แต่ละหมวด = โปสเตอร์ + รายการเครื่องดื่ม+ราคา

---

## 5. เนื้อหาจริงจาก Canva (ตัวเลขสำคัญ)

**งบลงทุน**
| รายการ | จำนวน |
|---|---|
| ค่าแฟรนไชส์ | 75,900฿ |
| ค่าก่อสร้างร้าน / Kiosk | 120,000 – 150,000฿ |
| อุปกรณ์อื่นที่ต้องซื้อเพิ่ม | 15,000฿ |
| **รวมงบลงทุน** | **210,000 – 250,000฿** |

**ประมาณการคืนทุน** — คิดที่ราคาเฉลี่ย 30฿/แก้ว เช่น ขาย 200 แก้ว/วัน → กำไรสุทธิ ~55,900฿/เดือน
(ตัวเลขทั้งตารางถอดจาก Canva และตรวจสอบความถูกต้องของการคำนวณแล้ว)

**การสนับสนุน** — โกดังกระจายสินค้า จ.เชียงใหม่ · อบรม 1 วัน · ระบบ POS Wongnai 16฿/วัน · จัดอุปกรณ์ล็อตแรก

---

## 6. ดีไซน์ & ฟอนต์ — ธีม "Boba Pop" 🧋

ปรับดีไซน์ทั้งหน้าเป็นทิศทาง **Boba Pop** (สนุก สดใส วัยรุ่น) หลังเลือกจาก mockup 3 แบบ
(ดูตัวเลือกที่ไม่ได้ใช้ใน [mockups/](mockups/) — B: Tea House Editorial, C: Night Market Glow)

- **โทนสี:** ครีม `#FFF7EC` · ส้ม `#F2913D` · เบจ `#E8C9A0` · มัทฉะ `#8FB43A` · น้ำตาลเข้ม `#2A1B12`
- **ฟอนต์หัวข้อ:** Mitr (กลมมน) · **เนื้อหา:** Prompt · ราคา/ตัวเลขใช้ Prompt (กัน ฿ ทับเลข)
- **จุดเด่น (signature):** Hero มีรูปวงกลม + blob ขยับได้ + ไข่มุก (pearls) ลอยรอบ ๆ
- การ์ดมุมโค้งใหญ่ ขอบเบจ hover ยก/เอียงเล่น ๆ · ป้ายหมวดเมนูสีมัทฉะ · เครื่องหมายถูกในวงกลม
- responsive mobile-first · carousel ปัดได้ · ตารางคืนทุนเลื่อนแนวนอน (คอลัมน์แรกตรึง)

---

## 7. วิธีใช้งานต่อ

**แก้เนื้อหา/ราคา/รูป** → เปิด `default-content.js` แก้ค่าในนั้น
**เพิ่มรูป** → วางไฟล์ใน `assets/` แล้วใส่ path เช่น `"assets/13.jpg"` ใน `branches.images`
**ขึ้นเว็บจริง**
```bash
npm i -g firebase-tools
firebase login
# ใส่ project id ใน .firebaserc
firebase deploy
```
ดูรายละเอียดเพิ่มใน [README.md](README.md)

---

## 8. การทดสอบที่ผ่านแล้ว ✅

- ทุก section แสดงครบ โทนสี/ฟอนต์ตรงแบรนด์ (เดสก์ท็อป + มือถือ 390px)
- carousel สาขา 12 รูป เลื่อน/ปัด/ลูกศร/จุด/auto-advance ทำงาน
- โลโก้แสดงถูกต้อง · section เมนูว่างถูกซ่อน
- งบลงทุน/ตารางคืนทุน = ตัวเลขจริง · ตาราง 9 คอลัมน์เลื่อนแนวนอนได้
- ฟอนต์ Google Sans/Kanit/Prompt โหลดและแสดงถูกต้อง
- console ไม่มี error (มีแค่ favicon 404 ที่ไม่กระทบ)

---

## 9. สิ่งที่ไม่ได้ทำ (ขอบเขต)

- ไม่มีระบบหลังบ้าน/ฐานข้อมูล (เจ้าของร้านแก้เองผ่านหน้าเว็บไม่ได้ — ต้องแก้โค้ด)
- ไม่มีฟอร์มเก็บรายชื่อผู้สนใจ (ใช้ปุ่มลิงก์ตรงไป LINE/Facebook แทน)
- ยังไม่ได้ deploy ขึ้น production (รอเจ้าของใส่ project id + deploy)
- ยังไม่ได้ใส่ลิงก์ LINE/Facebook จริง (ใช้ค่าตัวอย่าง `@taruka` — เปลี่ยนใน `contact`)
